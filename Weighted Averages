#include <stdio.h>
 
int main() {
int n,i;
float a,b,c,avg;
scanf("%d",&n);
for(i=1; i<=n;i++){
scanf("%f",&a);
scanf("%f",&b);
scanf("%f",&c);
avg = ((a*2)+(b*3)+(c*5))/(2+3+5);
printf("%.1f\n",avg);
    }
    return 0;
}
