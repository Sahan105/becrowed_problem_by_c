#include <stdio.h>
int main() {
int x,z,i,sum=0,j=0;
scanf("%d",&x);
while(1){
    scanf("%d",&z);
      if(z>x)
        break;
}
for(i=x;i<z;i++){
 sum += i;
     j++;
 if(sum >z)
 {
     break; 
 }
}
printf("%d\n",j);
return 0;
}
