#include <stdio.h>
 
int main() {
 
int n,i,sum=0, count=0;
while(1){
    scanf("%d",&n);
    if(n <0){
        break;
    }
    else{
        sum+=n;
        count++;
    }
}
 
 printf("%.2f\n",(float)sum/(float)count);
    return 0;
}
